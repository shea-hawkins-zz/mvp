import React from 'react';
import Header from './resources/layout/Header';
import VideoPlayer from './resources/videoPlayer/component';

export default (props) => {
  return (
    <div>
      <Header />
      <VideoPlayer />
    </div>
  );
};
